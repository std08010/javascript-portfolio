JavaScript is: 
high-level, dynamic, interpreted, object-oriented, functional
variables are untyped
derives from Scheme and Self.
was created at Netscape (now Mozilla)
ECMAScript aka ES
Since ES6 (2015), the ECMAScript specification has moved to a yearly release cadence like ES2016 etc.
pre-ES5 versions are legacy and no longer used.
after ES5 programs can opt in to JavaScript’s strict mode
The original host environment for JavaScript was a web browser. Since 2010 Node is used as host enviromnent for implementing web servers.

Lexical Structure:
